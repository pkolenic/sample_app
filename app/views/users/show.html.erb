<% provide(:title, @user.name) %>
<div class="row">
  <aside class="span4">
    <section>
      <h1>
        <%= gravatar_for @user, size: 80 %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>

<%-# render 'statbox' -%>
</div>
<% if signed_in? %>
	<%= render 'shared/disqus' %>
<% end %>