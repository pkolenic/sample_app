The tournament "<%= @tournament.name %>" no longer has the minimum team size needed
===============================================
<%= @owner.wot_name%>,


	<%= @tournament.name %> no longer has the minimum team size of <%= @tournament.minimum_team_size %>
	
	The current Team is:
    <% @team.each do |member| %>
        <%= member.wot_name %>
    <% end %>

    Please visit the official team page for '<%= @tournament.team_name %>' <%= @tournament.wot_team_link %>
    to verify that it reflects the same team as listed on <%= tournament_url @tournament, host: 'https://fearthefallen.herokuapp.com' %>
