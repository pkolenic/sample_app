	var disqus_config = function() {
		// The generated payload which authenticates users with Disqus
		this.page.remote_auth_s3 = '<%= sso_auth current_user %>';
		this.page.api_key = '<%= ENV['DISQUS_PUBLIC'] %>';
	}