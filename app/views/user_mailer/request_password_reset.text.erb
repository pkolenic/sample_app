Someone has request a password reset for <%= @user.name %>
===============================================

<%= @user.name %>, i

	If you did not requested a password reset then ignore this email.

	If you did request a password, then follow the following link within 6hrs.

	<%= reset_password_url @user, host: ALLIANCE_WEBSITE %>/token="#{@token}"
