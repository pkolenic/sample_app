<% provide(:title, "Edit Tournament") %>
<%= tinymce_assets %>
<h1>Update the Tournament</h1>

<div class="row">
  <div class="span8">
    <%= form_for(@tournament) do |f| %>
	  <%= render 'fields', f: f %>
	  <br/>
      <%= f.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>

<%= tinymce %>